---
phase: 07-init-wizard-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/wizard.ts
  - tests/wizard.test.ts
autonomous: true

must_haves:
  truths:
    - "Symlink pointing to correct target shows as 'already linked' (green)"
    - "Symlink pointing to wrong location shows as 'wrong target' with actual path"
    - "Real file (not symlink) shows as 'conflict'"
    - "New symlink (target doesn't exist) shows as 'new'"
  artifacts:
    - path: "src/wizard.ts"
      provides: "Fixed previewSymlinks function with proper target verification"
      contains: "readlink"
    - path: "tests/wizard.test.ts"
      provides: "Tests for symlink status detection"
      exports: []
  key_links:
    - from: "src/wizard.ts:previewSymlinks"
      to: "readlink + resolve"
      via: "symlink target verification"
      pattern: "readlink.*resolve"
---

<objective>
Fix the false conflict detection bug in the init wizard's symlink preview.

Purpose: The current `previewSymlinks` function detects symlinks but doesn't verify where they point. A symlink pointing to the wrong location incorrectly shows as `[exists]` instead of being flagged for attention. This is requirement INIT-04.

Output: Updated `previewSymlinks` function that correctly identifies:
- Already linked (symlink pointing to correct target)
- Wrong target (symlink pointing elsewhere)
- Conflict (real file, not symlink)
- New (target doesn't exist)
</objective>

<execution_context>
@/Users/brendon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-init-wizard-fixes/07-CONTEXT.md
@.planning/phases/07-init-wizard-fixes/07-RESEARCH.md
@src/wizard.ts
@index.ts (for resolveSymlinkTarget pattern at lines 228-233)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix previewSymlinks symlink target verification</name>
  <files>src/wizard.ts</files>
  <action>
Update the `previewSymlinks` function (around lines 1109-1167) to properly verify symlink targets:

1. When a target is detected as a symlink (line 1137), read where it actually points using `readlink`
2. Resolve the symlink target to absolute path using `resolve(dirname(target), linkTarget)`
3. Compare resolved target with expected source path
4. Set appropriate status:
   - If points to correct source: `[already linked]` (this is the happy path)
   - If points to different location: `[wrong target]` (user needs to decide)
   - Keep `[conflict]` for non-symlink files that exist

Pattern to follow from index.ts lines 228-233:
```typescript
async function resolveSymlinkTarget(linkPath: string): Promise<string> {
  const raw = await readlink(linkPath);
  return resolve(dirname(linkPath), raw);
}
```

The function already imports `readlink` and `resolve` so no new imports needed.

Update console output to show:
- `[already linked]` for correct symlinks (can show in green later)
- `[wrong target]` for symlinks pointing elsewhere (show actual target path)
- `[conflict]` for real files
- `[new]` / `[will create]` unchanged

Also update the return type to include `wrongTarget` items for proper handling upstream.
  </action>
  <verify>
Run `bun test` to ensure existing tests pass. The fix should not break any existing functionality.
  </verify>
  <done>
The `previewSymlinks` function correctly differentiates between symlinks pointing to correct target vs wrong target vs real file conflicts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for symlink status detection</name>
  <files>tests/wizard.test.ts</files>
  <action>
Create or extend `tests/wizard.test.ts` to test the symlink detection scenarios:

1. Test setup: Create temp directories for dotfiles repo and home simulation
2. Test cases:
   - Correct symlink: Create symlink pointing to expected source -> expect `[already linked]`
   - Wrong target symlink: Create symlink pointing to different file -> expect `[wrong target]`
   - Real file conflict: Create regular file at target location -> expect `[conflict]`
   - New target: No file at target location -> expect `[new]`

Use Bun's test utilities (`describe`, `test`, `expect`).
Use `mkdtemp` for temp directories and clean up in `afterEach`.

Test the `previewSymlinks` function directly by importing it. Note it logs to console, so tests should verify the return value (safe/hasConflicts) and can use mock/spy on console.log if needed to verify output.

Keep tests focused on the symlink detection logic - don't need to test all wizard flows.
  </action>
  <verify>
Run `bun test tests/wizard.test.ts` - all new tests pass.
  </verify>
  <done>
Test coverage exists for all four symlink status scenarios: already linked, wrong target, conflict, and new.
  </done>
</task>

</tasks>

<verification>
- `bun test` passes all tests
- Manual verification: Run `dot init` with an existing dotfiles repo that has some symlinks already set up. Symlinks pointing to correct locations should show `[already linked]`, not generic `[exists]`.
</verification>

<success_criteria>
- The false positive conflict detection bug is fixed
- Symlinks to correct targets are clearly distinguished from wrong targets
- Test coverage validates the fix
- No regressions in existing init wizard functionality
</success_criteria>

<output>
After completion, create `.planning/phases/07-init-wizard-fixes/07-01-SUMMARY.md`
</output>
