---
phase: 08.1-symlink-path-fix
plan: 01
subsystem: init
tags: [symlink, tilde-expansion, filesystem, path-resolution]

# Dependency graph
requires:
  - phase: 07-init-wizard-fixes
    provides: init wizard, previewSymlinks, installLinks
provides:
  - "Tilde expansion for all symlink filesystem operations"
  - "Correct symlink creation at actual home directory locations"
affects: [09-brewfile-sync, init-command]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "expandPath(target) before any filesystem operation on config paths"

key-files:
  created: []
  modified:
    - src/init.ts
    - src/wizard.ts

key-decisions:
  - "Import expandPath from wizard.ts (no duplication)"
  - "Keep ~ format in return values and display for portability"

patterns-established:
  - "Always expand ~ paths before lstat/readlink/mkdir/symlink operations"
  - "Use unexpanded paths for display and config storage"

# Metrics
duration: 2min
completed: 2026-02-02
---

# Phase 8.1 Plan 01: Symlink Path Fix Summary

**Fix critical bug where installLinks, getConflicts, getWrongTargets, and previewSymlinks pass ~ paths directly to filesystem operations without expansion**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-02T05:47:59Z
- **Completed:** 2026-02-02T05:49:35Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Fixed installLinks() to expand ~ before mkdir/symlink operations
- Fixed getConflicts() to expand ~ before lstat checks
- Fixed getWrongTargets() to expand ~ before lstat/readlink checks
- Fixed previewSymlinks() to expand ~ before pathExists/lstat/readlink checks
- All tests pass (218/218)

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix tilde expansion in init.ts functions** - `84beaf9` (fix)
2. **Task 2: Fix tilde expansion in previewSymlinks** - `59a3c7a` (fix)

## Files Created/Modified
- `src/init.ts` - Import expandPath, use in installLinks/getConflicts/getWrongTargets
- `src/wizard.ts` - Use expandPath in previewSymlinks before filesystem operations

## Decisions Made
- Import expandPath from wizard.ts rather than duplicating the helper
- Preserve ~ format in return values (items array) and display for config portability
- Only expand paths right before filesystem calls (lstat, readlink, pathExists, mkdir, symlink)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Symlink path bug fixed, init wizard now creates symlinks at correct locations
- Ready for Phase 9 (Brewfile Sync UX & Commands)
- All existing tests pass

---
*Phase: 08.1-symlink-path-fix*
*Completed: 2026-02-02*
