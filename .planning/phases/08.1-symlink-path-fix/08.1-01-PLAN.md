---
phase: 08.1-symlink-path-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/init.ts
  - src/wizard.ts
autonomous: true

must_haves:
  truths:
    - "installLinks() expands ~ to actual home directory before creating symlinks"
    - "getConflicts() uses expanded paths when checking for file conflicts"
    - "getWrongTargets() uses expanded paths when checking symlink targets"
    - "previewSymlinks() uses expanded paths for all lstat() and pathExists() calls"
    - "dot init creates symlinks at correct locations like /Users/brendon/.zshenv"
  artifacts:
    - path: "src/init.ts"
      provides: "Tilde expansion in installLinks, getConflicts, getWrongTargets"
      contains: "expandPath"
    - path: "src/wizard.ts"
      provides: "Tilde expansion in previewSymlinks"
      contains: "expandPath(target)"
  key_links:
    - from: "src/init.ts"
      to: "src/wizard.ts"
      via: "import { expandPath }"
      pattern: "import.*expandPath.*from.*wizard"
---

<objective>
Fix critical bug where init wizard creates symlinks in literal `~/` folder instead of expanding to home directory.

Purpose: Config stores paths with `~` for portability. Four functions pass these paths directly to filesystem operations without expansion, causing symlinks to be created at wrong locations (e.g., `/Users/brendon/.dotfiles/~/.zshenv` instead of `/Users/brendon/.zshenv`).

Output: All symlink-related functions properly expand `~` before filesystem operations.
</objective>

<execution_context>
@/Users/brendon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brendon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08.1-symlink-path-fix/08.1-CONTEXT.md
@src/init.ts
@src/wizard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tilde expansion in init.ts functions</name>
  <files>src/init.ts</files>
  <action>
Import `expandPath` from `./wizard` at the top of the file (add to existing imports from wizard).

Update three functions to expand `~` in target paths:

1. **installLinks()** (~line 155-187):
   - Add `const expandedTarget = expandPath(target);` after the for loop declaration
   - Replace all occurrences of `target` with `expandedTarget` for filesystem operations:
     - `mkdir(dirname(expandedTarget), { recursive: true })`
     - `lstat(expandedTarget)`
     - `readlink(expandedTarget)`
     - `symlink(source, expandedTarget)`
   - Keep `target` for display/logging purposes

2. **getConflicts()** (~line 101-121):
   - Add `const expandedTarget = expandPath(target);` inside the for loop
   - Replace `lstat(target)` with `lstat(expandedTarget)`
   - Keep `target` in the returned object for config reference

3. **getWrongTargets()** (~line 127-149):
   - Add `const expandedTarget = expandPath(target);` inside the for loop
   - Replace `lstat(target)` and `readlink(target)` with expanded versions
   - Replace `dirname(target)` with `dirname(expandedTarget)` for resolving symlink target
   - Keep `target` in the returned object for config reference

Do NOT duplicate the expandPath function - import it from wizard.ts where it already exists.
  </action>
  <verify>
Run `bun test` - all tests should pass.
Grep for "expandPath" in init.ts to confirm import and usage.
  </verify>
  <done>
init.ts imports expandPath from wizard and uses it in all three functions before any filesystem operation on target paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix tilde expansion in previewSymlinks</name>
  <files>src/wizard.ts</files>
  <action>
Update `previewSymlinks()` function (~line 1347-1496) to expand target paths before filesystem checks:

1. Inside the for loop iterating over `Object.entries(links)`, add:
   ```typescript
   const expandedTarget = expandPath(target);
   ```

2. Replace all filesystem operations on `target` with `expandedTarget`:
   - `pathExists(expandedTarget)` instead of `pathExists(target)`
   - `lstat(expandedTarget)` instead of `lstat(target)`
   - `readlink(expandedTarget)` instead of `readlink(target)`
   - `dirname(expandedTarget)` instead of `dirname(target)` when resolving symlink

3. Keep `target` (unexpanded, with `~`) in the returned `items` array for config consistency - the preview result should reflect what's in the config.

4. For display purposes, continue using `target` in the output since it shows the portable path format.

Note: `expandPath` is already defined in this file at line 236, so no import needed.
  </action>
  <verify>
Run `bun test` - all tests should pass.
Manually verify previewSymlinks contains `expandPath(target)` calls before filesystem operations.
  </verify>
  <done>
previewSymlinks() expands all target paths before pathExists(), lstat(), and readlink() calls while preserving `~` format in return values and display.
  </done>
</task>

</tasks>

<verification>
1. `bun test` passes (all existing tests)
2. Manual code review confirms:
   - init.ts imports expandPath from wizard
   - installLinks uses expandPath before mkdir/symlink
   - getConflicts uses expandPath before lstat
   - getWrongTargets uses expandPath before lstat/readlink
   - previewSymlinks uses expandPath before pathExists/lstat/readlink
</verification>

<success_criteria>
1. All four functions (installLinks, getConflicts, getWrongTargets, previewSymlinks) expand `~` to home directory before filesystem operations
2. `bun test` passes
3. No duplication of expandPath helper - init.ts imports from wizard.ts
4. Return values and display still use `~` format for portability
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-symlink-path-fix/08.1-01-SUMMARY.md`
</output>
